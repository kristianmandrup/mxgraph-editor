import { Images } from "../../editor";

describe("Images", () => {
  describe("static", () => {
    describe("properties", () => {
      describe("transparentImage", () => {
        it("is set", () => {
          expect(Images.transparentImage).toBeDefined();
        });
      });

      describe("moveImage", () => {
        it("is set", () => {
          expect(Images.moveImage).toBeDefined();
        });
      });

      describe("helpImage", () => {
        it("is set", () => {
          expect(Images.helpImage).toBeDefined();
        });
      });

      describe("checkmarkImage", () => {
        it("is set", () => {
          expect(Images.checkmarkImage).toBeDefined();
        });
      });

      describe("maximizeImage", () => {
        it("is set", () => {
          expect(Images.maximizeImage).toBeDefined();
        });
      });

      describe("zoomOutImage", () => {
        it("is set", () => {
          expect(Images.zoomOutImage).toBeDefined();
        });
      });

      describe("zoomInImage", () => {
        it("is set", () => {
          expect(Images.zoomInImage).toBeDefined();
        });
      });

      describe("zoomFitImage", () => {
        it("is set", () => {
          expect(Images.zoomFitImage).toBeDefined();
        });
      });

      describe("zoomFitImage", () => {
        it("is set", () => {
          expect(Images.zoomFitImage).toBeDefined();
        });
      });
      describe("layersImage", () => {
        it("is set", () => {
          expect(Images.layersImage).toBeDefined();
        });
      });
      describe("previousImage", () => {
        it("is set", () => {
          expect(Images.previousImage).toBeDefined();
        });
      });
      describe("nextImage", () => {
        it("is set", () => {
          expect(Images.nextImage).toBeDefined();
        });
      });
      describe("editImage", () => {
        it("is set", () => {
          expect(Images.editImage).toBeDefined();
        });
      });

      describe("zoomOutLargeImage", () => {
        it("is set", () => {
          expect(Images.zoomOutLargeImage).toBeDefined();
        });
      });
      describe("zoomInLargeImage", () => {
        it("is set", () => {
          expect(Images.zoomInLargeImage).toBeDefined();
        });
      });

      describe("actualSizeLargeImage", () => {
        it("is set", () => {
          expect(Images.actualSizeLargeImage).toBeDefined();
        });
      });
      describe("printLargeImage", () => {
        it("is set", () => {
          expect(Images.printLargeImage).toBeDefined();
        });
      });
      describe("layersLargeImage", () => {
        it("is set", () => {
          expect(Images.layersLargeImage).toBeDefined();
        });
      });

      describe("closeLargeImage", () => {
        it("is set", () => {
          expect(Images.closeLargeImage).toBeDefined();
        });
      });
      describe("editLargeImage", () => {
        it("is set", () => {
          expect(Images.editLargeImage).toBeDefined();
        });
      });
      describe("previousLargeImage", () => {
        it("is set", () => {
          expect(Images.previousLargeImage).toBeDefined();
        });
      });

      describe("nextLargeImage", () => {
        it("is set", () => {
          expect(Images.nextLargeImage).toBeDefined();
        });
      });
      describe("refreshLargeImage", () => {
        it("is set", () => {
          expect(Images.refreshLargeImage).toBeDefined();
        });
      });
      describe("backLargeImage", () => {
        it("is set", () => {
          expect(Images.backLargeImage).toBeDefined();
        });
      });
      describe("fullscreenLargeImage", () => {
        it("is set", () => {
          expect(Images.fullscreenLargeImage).toBeDefined();
        });
      });
    });
  });
});
