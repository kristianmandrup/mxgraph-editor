import { Editor } from "../../editor";

const chromeless = true;
const themes = [];
const mode = "x";
const editable = true;
const graph = {};

export const editor = new Editor(chromeless, themes, mode, graph, editable);
